# stub Widgets SDK Demo

This is a demo of a vanilla implementation of the stub Widgets SDK. In `index.html` add your App ID, Business ID and Token to the script at the bottom of the `<body>` section. 